export interface DataInfo {
  headline: string;
  title: string;
  description: string;
  button: {
    title: string;
    link: string;
  }
}

export interface DescriptionInfo {
  headline: string;
  title: string;
  description: string;
}

export interface CarouselImage {
  image: string;
}